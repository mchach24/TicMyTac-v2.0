var position;
var x;
var y;
if (event.pageX || event.pageY) {
	x = event.pageX;
	y = event.pageY;
}
else {
	x = event.clientX + document.body.scrollLeft + document.documentElement.scrollLeft;
	y = event.clientY + document.body.scrollTop + document.documentElement.scrollTop;
}
x -= subGame.offset().left + parseInt(subGame.css("padding-left").replace("px",""));
y -= subGame.offset().top + parseInt(subGame.css("padding-top").replace("px",""));
console.log("(" + x + "," + y + ")");